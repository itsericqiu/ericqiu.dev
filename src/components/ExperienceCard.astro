---
interface Props {
  company: string;
  roles: { title: string; dates: string }[];
  description: string;
  href: string;
  logoAlt: string;
  logoSrc?: string;
  icon: string;
  invertOnDark?: boolean;
  animDelay?: number;
}

const { company, roles, description, href, logoAlt, logoSrc, icon, invertOnDark = false, animDelay = 0 } = Astro.props;
---

<a href={href} target="_blank" rel="noopener noreferrer" class="card reveal" style={`transition-delay: ${animDelay}ms`}>
  <div class="card-top">
    <div class="icon-col">
      <span class="icon-button">
        <i class={icon}></i>
      </span>
    </div>
    <div class="title-col">
      <h3 class="company-name">{company}</h3>
    </div>
  </div>
  <div class="roles">
    {roles.map((role) => (
      <p class="role-line">
        <span class="role-title">{role.title}</span>
        {" "}
        <span class="role-dates">{role.dates}</span>
      </p>
    ))}
  </div>
  <p class="description">{description}</p>
  {logoSrc && (
    <div class="logo-wrapper">
      <img src={logoSrc} alt={logoAlt} class:list={["company-logo", { "invert-on-dark": invertOnDark }]} loading="lazy" />
    </div>
  )}
</a>

<style>
  .card {
    display: block;
    padding: 28px 32px;
    background: var(--bg-card);
    border-radius: 10px;
    box-shadow: var(--card-shadow);
    transition: all var(--transition);
  }

  .card:hover {
    transform: translateY(-2px);
    box-shadow: 0px 8px 12px -2px rgba(16,24,40,0.05), 0px 20px 24px -4px rgba(16,24,40,0.12);
  }

  .card-top {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1.25rem;
  }

  .icon-col {
    flex-shrink: 0;
  }

  .icon-button {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 48px;
    height: 48px;
    border-radius: 999px;
    background: var(--accent);
    color: #ffffff;
    font-size: 1.25rem;
    box-shadow: var(--button-shadow);
  }

  .title-col {
    flex: 1;
  }

  .company-name {
    font-family: var(--font-mono);
    font-size: 1.3rem;
    font-weight: 700;
    color: var(--accent);
  }

  .roles {
    margin-bottom: 1.25rem;
  }

  .role-line {
    font-size: 1.125rem;
    line-height: 1.4;
  }

  .role-title {
    color: var(--text-secondary);
  }

  .role-dates {
    color: var(--accent-dark);
  }

  .description {
    font-size: 0.95rem;
    color: var(--text-primary);
    line-height: 1.65;
    margin-bottom: 1.5rem;
  }

  .logo-wrapper {
    display: flex;
    justify-content: center;
    padding: 1rem 0 0.5rem;
  }

  .company-logo {
    max-width: 280px;
    width: 65%;
    height: auto;
    object-fit: contain;
  }

  @media (max-width: 768px) {
    .card {
      min-height: auto;
    }

    .role-line {
      font-size: 1rem;
    }
  }

  @media (max-width: 480px) {
    .card {
      padding: 20px 24px;
    }

    .company-logo {
      max-width: 180px;
    }
  }
</style>
